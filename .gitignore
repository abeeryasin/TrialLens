.env
*.db
__pycache__/
.venv/
course-alignment.md
.DS_Store
.claude/settings.local.json
.neon
.env.local
.agents/
.claude/skills/
.claude/hooks/
skills-lock.json
.ranking_cache/

# Skills installed into the repo root by an installer, not project content.
SKILL.md
**/.claude/skills/

# Seeded design canvas: ~2.5MB of embedded editor, rebuilt from design/*.dc.html
design/triallens-the-watch.html

# A cache for the interface-design skill, not a source of truth.
# Every value in it is already a comment beside the code it explains in
# frontend/charts.py, and this repo has already learned that a separate
# doc file goes stale and gets read only when someone remembers it
# exists (CLAUDE.md, on why there is no handoff file).
.interface-design/
